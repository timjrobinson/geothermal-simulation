## Important

- Commit after every major change
- Never use `git commit --amend` always create a new commit. 
- Never used `git commit -A` or other methods of committing all, only commit what you changed. 
- Never combine `cd` and `git` commands. Do them as separate commands. 